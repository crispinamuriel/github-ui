<h3>Github Organizations List</h3>
<ul>
{{#each this.model as |org|}}
    <li>
        <span {{action 'addToFavorites' org}}>Add to Favorites</span>
        {{link-to org.id 'org' org.id}}
    </li>
{{/each}}
</ul>
{{outlet}}
